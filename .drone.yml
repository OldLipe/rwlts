kind: pipeline
name: default

steps:
- name: test
  image: m3nin0/r-client:4.0.3_fix_packages
  commands:
  - R CMD build .
  - R CMD check *tar.gz --as-cran

- name: coverage
  image: m3nin0/r-client:4.0.3_fix_packages
  environment:
    CODECOV_TOKEN:
      from_secret: codecov_token
  commands:
  - R -e 'library(covr); codecov(quiet = FALSE)'
  when:
    event:
    - push

- name: discord-notify
  image: brazildatacube/bdc-drone-discord
  settings:
    webhook:
      from_secret: discord_webhook
  when:
    status:
      - failure
      - success
